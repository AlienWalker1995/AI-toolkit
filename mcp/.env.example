# MCP Gateway secrets — these now live in the root .env file, not here.
# Add the following keys to your root .env:
#
#   GITHUB_PERSONAL_ACCESS_TOKEN=ghp_...
#   BRAVE_API_KEY=...
#
# docker-compose.yml passes them into the mcp-gateway container automatically.
# This file is kept as a reference only.
